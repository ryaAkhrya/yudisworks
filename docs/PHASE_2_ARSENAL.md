# Phase 2: Core Sections (Layout)

**Goal:** Build the static structure of the website using asymmetrical design.

**Tasks:**
1. Build `<Hero />`: Massive skewed text ("I'LL STEAL YOUR DEADLINES"), overlapping elements, hard-shadow CTA button ("Drop Your Request").
2. Build `<Services />`: Render data from `PORTFOLIO_DATA.md` (Indonesian descriptions). Scatter the 3 cards unevenly. Apply thick black borders and `-5deg` skew. 
3. Ensure mobile responsiveness: Elements can stack but must retain jagged borders and hard shadows.