# Phase 7: Optimization & Deploy

**Goal:** Ship `yudisworks` to production.

**Tasks:**
1. Configure Next.js Metadata (Title, Description, OpenGraph image).
2. Audit for layout shifts (CLS) to ensure GSAP animations don't break the document flow.
3. Prepare `.env.example` with Supabase keys.
4. Setup Vercel deployment.