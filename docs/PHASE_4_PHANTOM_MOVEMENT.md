# Phase 4: GSAP & Interactions

**Goal:** Bring the site to life with aggressive P5 animations.

**Tasks:**
1. Implement `<Marquee />`: GSAP infinite scrolling text background (Text: '/// THE ARSENAL /// WHAT I DO ///').
2. Add enter animations to `<Hero />` and `<Services />` using GSAP ScrollTrigger (snappy slide-ins).
3. Implement Hover States: Element jitter/shake effect and sharp color inversion (e.g., Red to Black instantly) on buttons and cards using GSAP.